doctype
html(lang="es")
	head
		meta(charset="UTF-8")
		title Código (for/each)
	body
		h1 Código (for/each)
		- var colores = ['purple', 'orange', 'teal', 'green']
		ul
			each color in colores
				li(style={color: color})= color

		table(border=1)
			- for(var i = 1; i <= 10; i++)
				tr
					- for(var j = 1; j <= 20; j++)
						td f#{i} c#{j}
	